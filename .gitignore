# Composer
/vendor/
# composer.lock is committed on purpose: without it GitHub's dependency graph
# only sees composer.json's version ranges, and Dependabot cannot raise a
# security alert against a range. Consumers ignore a library's lock anyway.

# PHPUnit
/coverage/
.phpunit.result.cache

# PHPStan
/phpstan.result

# PHP CS Fixer
.php-cs-fixer.cache

# IDE
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# Build
/build/
/dist/

# Environment
.env
.env.local
